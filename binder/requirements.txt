# Environment for mybinder.org, which runs this repo as a live JupyterLab.
# Kept in step with pyproject.toml; uv.lock is the authoritative pin for local work.
numpy
pandas
matplotlib
seaborn
scikit-learn
jupyterlab
# install this repo so notebooks can `from cs156.data import iris`
-e .
